@page
@model FunWithBrandt.ProgramsModel
@{
    ViewData["Title"] = "Programs";
}

<head>
    <style>
        .language-logo {
            width:45px;
            height:45px;
        }

        .pre-scrollable {
            max-height: 450px;
        }

        .program-language {
            font-size: x-large
        }

        .program-title {
            font-size: large
        }

        .program-description {
        }

        .program-code {
        }
    </style>
</head>

<h2>Programs</h2>
<br />

<p>
    Follow me on GitHub: <a href="https://github.com/Brandt-moreThan4">https://github.com/Brandt-moreThan4 </a>
</p>
<p>
    Update: I think this page is actually kind of stupid. Any program worth showing is probably too complex to 
    dispaly in this format and should instead just be viewed on GitHub. Maybe I can make this page more about describing the 
    programs as a whole with snippets like below to highlite cool things?
</p>


<ul class="nav nav-tabs" id="myTab" role="tablist">
    <li class="nav-item">
        <a class="nav-link active" id="Python-tab" data-toggle="tab" href="#Python" role="tab" aria-controls="Python" aria-selected="true">Python</a>
    </li>
    <li class="nav-item">
        <a class="nav-link" id="CSharp-tab" data-toggle="tab" href="#CSharp" role="tab" aria-controls="CSharp" aria-selected="false">CSharp</a>
    </li>
    <li class="nav-item">
        <a class="nav-link" id="VBA-tab" data-toggle="tab" href="#VBA" role="tab" aria-controls="VBA" aria-selected="false">VBA</a>
    </li>
</ul>


<div class="tab-content" id="myTabContent">
    <div class="tab-pane fade show active" id="Python" role="tabpanel" aria-labelledby="Python-tab">
        <div>
            <img class="language-logo" src="~/Images/LanguageLogos/Python.png" />
        </div>
        @foreach (var post in Model.programPosts)
        {
            if (post.Language == "Python")
            {


                <div>
                    <h2 class="program-title">@post.Title</h2>
                    <p class="program-description">@post.Description</p>
                    <br />

                    <pre class="pre-scrollable prettyprint">
                    @post.Code
                </pre>

                </div>
                <hr />
                <br />
                <br />

            }
        }
    </div>
    <div class="tab-pane fade" id="CSharp" role="tabpanel" aria-labelledby="CSharp-tab">
        <div>
            <img class="language-logo" src="~/Images/LanguageLogos/CSharp.png" />
            <p>
                C# programs don't lend themselves as easily to just pasting in a block of code.
                The code for C# programs is typically spread out amongst many separate files with each only
                focusing on a small piece of the overall programm. I haven't yet figured out the best way to display these.
                Probably just a small description with a link to github, but if it seems understandable on its own I may just post
                individual classes here. 
            </p>
        </div>
        @foreach (var post in Model.programPosts)
        {
            if (post.Language == "C#")
            {


                <div>
                    <h2 class="program-title">@post.Title</h2>
                    <p class="program-description">@post.Description</p>
                    <br />

                    <pre class="pre-scrollable prettyprint">
                    @post.Code
                </pre>

                </div>
                <hr />
                <br />
                <br />

            }
        }
    </div>

    <div class="tab-pane fade" id="VBA" role="tabpanel" aria-labelledby="VBA-tab">
        <div>
            <img class="language-logo" src="~/Images/LanguageLogos/VBA.png" />
        </div>
        @foreach (var post in Model.programPosts)
        {
            if (post.Language == "VBA")
            {


                <div>
                    <h2 class="program-title">@post.Title</h2>
                    <p class="program-description">@post.Description</p>
                    <br />

                    <pre class="pre-scrollable prettyprint">
                    @post.Code
                </pre>

                </div>
                <hr />
                <br />
                <br />

            }
        }
    </div>
</div>


<script src="https://cdn.jsdelivr.net/gh/google/code-prettify@master/loader/run_prettify.js"></script>


