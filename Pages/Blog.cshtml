@page
@model FunWithBrandt.BlogModel
@{
    ViewData["Title"] = "Blog";
}

<head>
    <style>
        .blog {
            max-width:900px;
        }

        .blog-date {
            font-size: x-large;
        }

        .blog-title {
            font-size:small;
        }

        .blog-content {

        }

        .blog-image {
            max-height:500px;
            max-width:500px;
        }

    </style>
</head>

<h1>Some Blog Title Here</h1>
<br /><br />

<p>
    The following are just a collection of a few random notes I have written over the past few years
    on either random topics that were interesting to me or on books I was reading.
    I mainly just wanted to get a feel for how this page could look.
</p>
<br />

<div class="row">
    <div class="col-md-9">
         @foreach (var post in Model.blogPosts) { 
            <div class="blog">
                <h1 class="blog-date">@post.Title</h1>
                <h2 class="blog-title">@post.Date.ToLongDateString()</h2>
                <br />
                <div class="blog-content"> 

                @{ 
                    foreach (var chunk in post.Content)
                    {
                        if (!chunk.Contains("<img>"))
                        {
                            <p>@chunk</p>
                        }
                        else
                        {   <div>
                                <img class="img-fluid" @("src= " + chunk.Replace("<img>","")) />
                            </div>    
                        }
                    }
                }
                
                </div>
            </div>
            <hr />
            <br />
            <br />

        }
    </div>
</div>